# CHANGELOG — The Misfits

_Internal development log. Updated by Claude after every completed task. One row per change. Sessions are separated by a header. This is not the public release log — see RELEASES.md for versioned public updates._

---

## Session 1 — 2026-06-21

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 1 | Project initialized | CLAUDE.md, DESIGN.md, CHANGELOG.md, RELEASES.md, README.md created. Internal Docs/ folder set up with ARCHITECTURE.md, DATA_FLOW.md, and COMPONENTS.md. | Setup | All |
| 2 | Design tokens extracted from Figma | Color and typography tokens pulled from the main page Figma frame and added to DESIGN.md. 6 color tokens, 6 typography tokens, 8 spacing tokens defined. | Update | DESIGN.md |
| 3 | Next.js project scaffolded | Manually initialized Next.js 15 app with TypeScript, App Router, CSS Modules. Created package.json, next.config.ts, tsconfig.json, eslint config, app/layout.tsx, app/page.tsx, app/globals.css with all design tokens as CSS variables. | Setup | package.json, next.config.ts, tsconfig.json, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css |
| 4 | AboutSection component built | Built AboutSection from Figma frame. Header with Plus icon (Phosphor), two content rows with webring description text. Installed @phosphor-icons/react. | Feature | components/sections/AboutSection.tsx, components/sections/AboutSection.module.css |
| 5 | RequirementSection component built | Built RequirementSection from Figma frame. Header with Plus icon, content area with 7-item bulleted list of join requirements. | Feature | components/sections/RequirementSection.tsx, components/sections/RequirementSection.module.css |
| 6 | JoinSection component built | Built JoinSection from Figma frame. Header with Plus icon, single paragraph content block explaining how to join. | Feature | components/sections/JoinSection.tsx, components/sections/JoinSection.module.css |
| 7 | AuthorSection component built | Built AuthorSection from Figma frame (482px wide). Header with Plus icon, two-line content with accent-coloured link text for social links. | Feature | components/sections/AuthorSection.tsx, components/sections/AuthorSection.module.css |
| 8 | GuildSection component built | Built GuildSection from Figma frame (481px wide). Three sub-sections: guild info header + description, numbered members list (11 entries, name + URL in accent gold), update log footer bar with GitFork and CaretRight Phosphor icons. | Feature | components/sections/GuildSection.tsx, components/sections/GuildSection.module.css |
| 9 | GallerySection component built | Built GallerySection from Figma frame (1400px wide). Header with Plus icon, CSS marquee animation of 15 site screenshot images (duplicated for seamless loop, pauses on hover). Figma image URLs — placeholder until real member images added. | Feature | components/sections/GallerySection.tsx, components/sections/GallerySection.module.css |
| 10 | AnimationSection component built | Built AnimationSection from Figma frame (482px wide, 625px tall). Stats header with Eye + UserFocus + Plus Phosphor icons, Views and Members counters. Large 583px placeholder area for future GSAP animation. | Feature | components/sections/AnimationSection.tsx, components/sections/AnimationSection.module.css |
| 11 | Home screen assembled | Composed main page from all 7 section components. 1880px fixed layout with 20px page padding: GuildSection (left sidebar), AboutSection + RequirementSection + JoinSection (center column), AnimationSection + AuthorSection (right column), GallerySection (full-width bottom row). | Feature | app/page.tsx, app/page.module.css |
| 12 | Border collapse + viewport scaling | Fixed doubled borders: removed container-level borders from AboutSection and GallerySection (children's borders now form the outer edges). Added margin-top: -1px to non-first children in centerColumn and rightColumn. Created ScaleWrapper client component — scales the fixed 1920×1080 layout to fit any viewport using transform:scale() with resize listener. | Bug Fix | components/sections/AboutSection.module.css, components/sections/GallerySection.module.css, app/page.module.css, app/page.tsx, components/ScaleWrapper.tsx |
| 14 | GSAP particle interaction | Built CoolInteraction.tsx — canvas-based particle system with 120 floating ASCII chars, dots and numbers in accent gold. Hover slows particles via GSAP speed tween. Click-and-release fires a ripple wave that pushes nearby particles outward. Replaced placeholder image in AnimationSection. Installed gsap. | Feature | components/CoolInteraction.tsx, components/sections/AnimationSection.tsx, package.json |
| 13 | Width-fill scaling + member list numbering | Changed ScaleWrapper to scale by viewport width only (was using min of width/height, which left a gap on the right when browser chrome reduced viewport height). Members list numbering fixed: ol/li flex display overrides list-item and kills decimal markers — switched to block layout with margin-top spacing and display:block on name/url spans. | Bug Fix | components/ScaleWrapper.tsx, components/sections/GuildSection.module.css |

## Session 2 — 2026-06-22

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 1 | Data layer + ring mechanics | Installed gray-matter. Created content/sites/ with 5 dummy member markdown files. Built lib/sites.ts (reads + parses all .md files, returns typed Site[]). Built lib/ring.ts (getRingNeighbors returns prev/next by alphabetical slug with wrap-around). Wired getSites() into GuildSection replacing hardcoded MEMBERS array. | Feature | content/sites/*.md, lib/sites.ts, lib/ring.ts, components/sections/GuildSection.tsx, package.json |

## Session 3 — 2026-06-23

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 1 | New design tokens added | Added --color-white, --space-2-5 (10px), --space-4-5 (18px). Fixed --font-size-sm from 15px to 14px to match Figma. Updated globals.css and DESIGN.md. | Update | globals.css, DESIGN.md |
| 2 | FormSection built | Built FormSection from Figma frame (node 51:35). Two-part form with required fields (name, nickname, website, email, tags) and optional fields (bg color, text color, comments textarea), plus submit/discard action row. | Feature | components/sections/FormSection.tsx, components/sections/FormSection.module.css |
| 3 | Token --space-3-5 added | Added --space-3-5: 14px to globals.css and DESIGN.md. | Update | globals.css, DESIGN.md |
| 4 | SuccessSection built | Built SuccessSection from Figma frame (node 53:184). Three rows: confirmation message, iframe embed code field with copy icon, and action buttons (Go Back, Check Github Page, Contact Guild Leader). | Feature | components/sections/SuccessSection.tsx, components/sections/SuccessSection.module.css |
| 5 | PreviewSection built | Built PreviewSection from Figma frame (node 54:211). Two-column layout: left panel renders the selected widget preview (289×103px), right panel shows a formatted application number badge (e.g. Member Application [#012]). Accepts widgetPreview (ReactNode) and applicationNumber (number) props. | Feature | components/sections/PreviewSection.tsx, components/sections/PreviewSection.module.css |
| 6 | Token --space-5 added | Added --space-5: 20px to globals.css and DESIGN.md. | Update | globals.css, DESIGN.md |
| 7 | WidgetTicker built | Built WidgetTicker from Figma frame (node 54:222). Client component carousel — shows prev/center/next widgets with caret navigation. Center is selected (white border, 289×103px); sides scale to 73.5% with opacity 0.5 and no border. Accepts widgets array and optional onSelect callback. | Feature | components/sections/WidgetTicker.tsx, components/sections/WidgetTicker.module.css |
| 8 | JoinGuildSection built | Built JoinGuildSection from Figma frame (node 54:232). Header bar (Join The Guild + Plus icon) and static welcome/instructions content row. | Feature | components/sections/JoinGuildSection.tsx, components/sections/JoinGuildSection.module.css |
| 9 | Token --font-weight-bold added | Added --font-weight-bold: 700 to globals.css and DESIGN.md. | Update | globals.css, DESIGN.md |
| 12 | WidgetTicker + divider fixes | JoinGuildSection content height 105→113px to align bottom divider with GuildSection. WidgetTicker rewritten: single widget display (no side widgets), registry-sourced (WIDGET_IDS array), 171→220px height, white carets with BACK/NEXT label on hover. CenterColumn cleaned up (no DEMO_WIDGETS). | Feature | JoinGuildSection.module.css, WidgetTicker.tsx, WidgetTicker.module.css, CenterColumn.tsx |
| 11 | Header + gap fixes | JoinGuildSection header corrected to 48px to match all other section headers. centerColumn set to align-self: stretch with last-child flex: 1 to fill full height. FormSection submitRow set to flex: 1 to absorb remaining space in join view. | Bug Fix | JoinGuildSection.module.css, page.module.css, FormSection.module.css |
| 14 | Success screen assembled | Added "success" view to CenterColumn (3rd state after join). Wired FormSection to POST /api/apply which creates the member MD file in /members. On success, shows JoinGuildSection + PreviewSection (real widget rendered with submitted nickname) + SuccessSection (iframe embed code with real slug). SuccessSection now accepts slug + onGoBack props. FormSection is now fully controlled with all fields tracked and a submit handler. | Feature | CenterColumn.tsx, FormSection.tsx, SuccessSection.tsx, src/app/api/apply/route.ts |
| 13 | Widget011 built | Dark (#141414) pill widget, 300×29px. Doto SemiBold font (added to layout.tsx). Gold identity row (nickname + GuildMark + "THE MAKERS GUILD"). Gold pixel-art << and >> carets (SVG dot arrays) with list icon for random nav. Registered in WidgetRenderer and added to WidgetTicker. Fixed Widget004/007 overflow (added overflow:hidden + identity flex:1) and Widget008 top-clip (removed overflow:hidden, height→min-height). | Feature | Widget011.tsx, Widget011.module.css, layout.tsx, WidgetRenderer.tsx, WidgetTicker.tsx, Widget004.module.css, Widget007.module.css, Widget008.module.css |
| 10 | Form screen assembled (client-side view swap) | Assembled the form screen as a client-side view swap within the homepage. CenterColumn.tsx manages "home"/"join" state. Home view: AboutSection + RequirementSection + JoinSection. Join view: JoinGuildSection + WidgetTicker + FormSection. JoinSection updated to accept onJoin prop and style "this form here" as bold gold clickable trigger. FormSection updated to accept onDiscard prop. page.tsx updated to use CenterColumn. | Feature | components/CenterColumn.tsx, components/sections/JoinSection.tsx, components/sections/JoinSection.module.css, components/sections/FormSection.tsx, app/page.tsx |

## Session 4 — 2026-06-23

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 1 | Member health check script + workflow | Created scripts/check-members.mjs — reads all members/*.md, fetches each site URL (10s timeout), checks homepage HTML for guild widget embed, and raises a removal PR for any member whose site is down or widget is missing. Created .github/workflows/member-health-check.yml — runs every Monday at midnight UTC and supports manual dispatch. Removal PRs land on branches like removal/SLUG-YYYYMMDD with a table of issues found and merge/close instructions. | Feature | scripts/check-members.mjs, .github/workflows/member-health-check.yml |

## Session 5 — 2026-07-22

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 1 | Iframe → script-based widget embed | New /widget/[slug] route reuses existing WidgetRenderer/Widget001-011 server-side, tagged with data-widget-state. New public/widget-loader.js vanilla-JS loader injects the widget into a Shadow DOM on the host page (CORS added for /widget/:path*). All 11 legacy widgets switched from relative to absolute ring nav links via new lib/site.ts (SITE_ORIGIN). Embed snippet on success pages switched from `<iframe>` to `<script>`. Legacy /embed/[slug] iframe route left untouched for existing members. | Feature | src/app/widget/[slug]/page.tsx, public/widget-loader.js, src/lib/site.ts, src/widgets/Widget001-011.tsx, next.config.ts, SuccessSection.tsx, MobileSuccessPage.tsx, JoinGuildSection.tsx, MobileFormPage.tsx |
| 2 | Widget last-seen ping tracking (file-based, no DB) | In-memory hit counter (src/lib/widgetHits.ts) bumped on every /widget/[slug] render, read via secret-gated /api/widget-hits (clears on read). New scripts/flush-last-seen.mjs cron script (GitHub Actions, every 4h) merges hits into committed last-seen.md, same table style as activity.md. Approximate/best-effort by design — serverless memory resets on cold starts. | Feature | src/lib/widgetHits.ts, src/app/api/widget-hits/route.ts, scripts/flush-last-seen.mjs, .github/workflows/flush-last-seen.yml, last-seen.md |
| 3 | New color token system from redesign | Replaced the single-gold-accent palette with a multi-accent system extracted from the Success Screen redesign frame (node 162:1832): bg/surface/border/text tokens updated, 5 new accent tokens added (yellow, purple, green, blue, pink) for color-coded label badges. | Update | internal/DESIGN.md, src/app/globals.css |
| 4 | Widget-001 (v2) built | First of the redesigned widget generation, built from Figma node 162:2203. Two-tier design: top-right "{nickname} @ misfits" badge with inline Misfits mark icon (exact SVG path committed, not redrawn), full-width "member of the webring" bar with [next] [rand] [prev] nav below it. Blue (#356aff) / white, Chivo Mono, uppercase, matches new accent-blue token. New src/widgets/v2/ folder (kebab-case filenames) kept separate from legacy src/widgets/. Added /widget-preview route (mirrors /widgets) to preview all v2 widgets. | Feature | src/widgets/v2/widget-001.tsx, src/widgets/v2/widget-001.module.css, src/widgets/v2/WidgetV2Renderer.tsx, src/widgets/v2/registry.ts, src/app/widget-preview/page.tsx |
| 5 | Widget-001 refinements | "member of the webring" label reduced to 18px. Top badge shifted 48px left of the bar's right edge (margin-right). Preview page nickname placeholder changed from IMTI to USER to match the Figma placeholder text. | Update | src/widgets/v2/widget-001.module.css, src/app/widget-preview/page.tsx |
| 6 | Widgets 002-009 (v2) built | Built the remaining 8 widgets in the new generation from Figma nodes 165:2257, 165:2268, 168:2288, 168:2289, 168:2298, 168:2312, 168:2345, 168:2444. Shared icon set factored into src/widgets/v2/icons.tsx (MisfitsMark, SkipIcon, ShuffleIcon — exact SVG paths, reused verbatim across widgets and confirmed byte-identical via md5 before reuse). Two nav styles: bracket-text ([prev][rand][next], widgets 002/003/005) and icon-button pill (widgets 004/006/007/008/009). All registered in WidgetV2Renderer + registry.ts, visible on /widget-preview. | Feature | src/widgets/v2/icons.tsx, src/widgets/v2/widget-002.tsx through widget-009.tsx (+ .module.css each), src/widgets/v2/WidgetV2Renderer.tsx, src/widgets/v2/registry.ts, src/widgets/v2/widget-001.tsx (refactored to use shared icons.tsx) |
| 7 | Widget v2 sizing + adaptability fixes | All icon-containing rows/pills (identity bars and icon-nav pills across widgets 001/002/003/004/005/006/008/009) now have an explicit 28px height with horizontal-only padding and box-sizing:border-box, replacing padding-driven variable heights. Widget-005's "{nickname} @ misfits" identity box changed from stretch-to-container-width to align-self:flex-start so it shrinks to fit its content instead of leaving empty space. Widgets 002/003/005's identity boxes (the ones showing the live nickname) now shrink-to-fit and can grow past their nominal wrapper width for longer real usernames, in preparation for the live username form preview. Widget-002/005's nav row also switched from a fixed-width, 23px-indented bar to shrink-to-fit, then center-aligned (align-items:center on the column container) against the identity box above/below it, matching how 004/006 already center their icon-nav pill under the full-width identity bar. | Bug Fix | src/widgets/v2/widget-001.module.css, widget-002.module.css, widget-003.module.css, widget-004.module.css, widget-005.module.css, widget-006.module.css, widget-007.module.css, widget-008.module.css, widget-009.module.css |
| 8 | HeaderTag component built | Built HeaderTag from Figma frame (node 187:290), first component of the new full-screen redesign. Small uppercase label pill in Chivo Mono, color driven by a `color` prop mapped to the 5 existing accent tokens (yellow/purple/green/blue/pink) so it can be reused across the header with different colors. Added 1 new token: `--font-size-md` (20px). | Feature | src/components/HeaderTag.tsx, src/components/HeaderTag.module.css, src/app/globals.css, internal/DESIGN.md |
| 9 | Fixed broken --font-family-mono token | Chivo Mono/Familjen Grotesk/Doto are next/font variables scoped to `<body>` (set via `.variable` class in layout.tsx), not `<html>`. A `:root` token referencing `--font-chivo-mono` silently resolved to nothing since `:root` (html) can't see variables defined on a descendant. Removed the token, switched HeaderTag to reference `var(--font-chivo-mono, 'Chivo Mono', monospace)` directly at point of use — matching every widget component already in the codebase. Documented the gotcha in DESIGN.md. | Bug Fix | src/app/globals.css, src/components/HeaderTag.module.css, internal/DESIGN.md |
| 10 | Fixed wrong production domain across the codebase | `the-misfits.vercel.app` (hardcoded as the SITE_ORIGIN/GUILD_ORIGIN fallback in 6 files) had been claimed by an unrelated third-party site, causing the flush-last-seen cron to fail parsing HTML as JSON. Root cause was domain drift after the Vercel project rename, not the HITS_FLUSH_SECRET as first suspected — a wrong/missing secret returns our own JSON 401, not HTML. Replaced every reference with the real domain, themisfits.byimti.tools. | Bug Fix | src/lib/site.ts, scripts/flush-last-seen.mjs, scripts/check-members.mjs, .github/workflows/flush-last-seen.yml, .github/workflows/member-health-check.yml, README.md |
| 11 | Removed NEXT_PUBLIC_SITE_URL env var dependency | SITE_ORIGIN in src/lib/site.ts is now a plain hardcoded constant instead of `process.env.NEXT_PUBLIC_SITE_URL ?? '...'`. Avoids a stale/wrong Vercel dashboard env var silently overriding the correct domain again in the future. | Refactor | src/lib/site.ts |
| 12 | MarqueeTag component built | Built MarqueeTag from Figma frame (node 187:365). Infinite left-scrolling marquee of joined messages, slow speed (0.5px/frame, duplicated content for seamless loop via requestAnimationFrame — same technique as GalleryTrack.tsx). Same color-prop pattern as HeaderTag (defaults to green, matching Figma). | Feature | src/components/MarqueeTag.tsx, src/components/MarqueeTag.module.css |
| 13 | NavTag component built + Gallery Button reused | Built NavTag from Figma frame (node 187:360) — neutral nav pill (bg var(--color-surface-alt), text var(--color-text-tertiary)), used for prev/next-style navigation labels. Figma's "Gallery Button" (node 187:342) was visually identical to the existing HeaderTag (same yellow, same structure) so no new component was built for it — it's just HeaderTag with label="gallery >". | Feature | src/components/NavTag.tsx, src/components/NavTag.module.css |
| 14 | Fixed HeaderTag text contrast on dark accents + StackedTag built | Building "Active Button" (Figma node 187:314, purple bg) revealed HeaderTag hardcoded black text for all 5 accent colors, contradicting DESIGN.md's own token usage notes (--color-white for dark/blue badges, --color-black for bright yellow/green badges). Fixed: blue/purple/pink variants now use white text, yellow/green stay black. Built StackedTag — two HeaderTag pills stacked vertically with zero gap, align 'start' or 'end', composed from Figma nodes 187:346 (Visit Button, left-aligned), 187:314 (Active Button, right-aligned), 187:351 (Join Button, left-aligned). No new pill styling needed — reuses HeaderTag entirely. | Bug Fix / Feature | src/components/HeaderTag.module.css, src/components/StackedTag.tsx, src/components/StackedTag.module.css |
| 15 | LogoBox component built | Built LogoBox from Figma frame (node 187:321) — site logo lockup: a zigzag vector mark (distinct from the widget MisfitsMark icon; exact path downloaded and committed verbatim, not redrawn) rotated -90deg, paired with StackedTag(color="blue") showing "the" / "misfits". Icon recolored via currentColor + var(--color-accent-blue), matching the original #356AFF fill exactly. | Feature | src/components/LogoBox.tsx, src/components/LogoBox.module.css |
| 16 | TaggedSection component built | Built one reusable component from Figma nodes 191:551 ("Sections Single") and 191:552 ("Sections Double") — they're the same layout (HeaderTag + N dark content boxes with 4px gaps), only differing in content box count, so no separate components were built. Added the plain "Chivo" font (distinct from Chivo Mono) via next/font, plus 2 new tokens: --font-size-body (16px) and --line-height-body (21px), for body copy specifically. Content box text confirmed to intentionally use --color-white (not the usual --color-text-primary grey) per the redesign. | Feature | src/components/TaggedSection.tsx, src/components/TaggedSection.module.css, src/app/layout.tsx, src/app/globals.css, internal/DESIGN.md |
| 17 | New homepage built from Figma (node 139:322), replacing the old design | Assembled the full redesigned homepage entirely from existing atoms: LogoBox, MarqueeTag, HeaderTag, StackedTag, TaggedSection. Widened TaggedSection's content prop from string[] to ReactNode[] to support real lists/links (member list, requirements bullets, inline social links) instead of just plain paragraphs. Added 4 new spacing tokens (--space-7:28px, --space-10-5:42px, --space-12-5:50px, --space-14:56px) for recurring layout gaps/padding. Dropped ScaleWrapper's fixed-1920-canvas + transform:scale approach for this screen — left/center/right columns now use true flex-grow ratios (356:899:479, matching Figma exactly) so they reflow with real viewport width instead of just uniformly scaling. Wired real data: getSites() for the member list (replacing Figma's placeholder), getViews() for the view counter, existing RELEASES.md-parsing helper for version, real social links reused from the old AuthorSection. "Days Online" and "Last Update" stats have no existing data source — left as static placeholders with TODO comments. Old design's section components (AboutSection, GuildSection, CenterColumn, etc.) are left untouched in the repo but no longer imported by page.tsx. Mobile view (MobileRoot/MobilePage) untouched. Downloaded and committed the exact header background texture asset from Figma rather than recreating it. | Feature | src/app/page.tsx, src/app/page.module.css, src/components/TaggedSection.tsx, src/components/TaggedSection.module.css, src/app/globals.css, internal/DESIGN.md, public/assets/homepage-bg-texture.png |
| 18 | Homepage column ratio adjusted | Left/center/right flex-grow ratio changed from 356:899:479 (exact Figma proportions) to 400:899:435 — left column widened, right column narrowed, per user request. Presented as 3 metric options (subtle/balanced/bold) per CLAUDE.md's metric-change rule; subtle was chosen. | Update | src/app/page.module.css |
| 19 | Homepage polish: link underlines, marquee looping, member list size, fluid type | (1) Removed browser-default underline from the join/github/gallery header pill links via new .headerLink class. (2) Fixed MarqueeTag's seamless-loop bug — it always rendered exactly 2 text copies regardless of container width, so wide flex-filled header slots (like the real homepage, vs the narrow demo box on /widget-preview) ran out of content mid-loop and visibly jumped/gapped. Now measures via ResizeObserver and duplicates enough copies to always cover 2x the visible width. (3) Member list font-size/line-height override removed from page.module.css — it now inherits TaggedSection's body typography (16px/1.3125) instead of the Figma-literal 14px/23px, matching the other sections as requested. (4) Made --font-size-md and --font-size-body fluid via clamp() tied to viewport width (16px→22px and 14px→18px respectively, scaling between 768px and 1920px viewports) — safe because both tokens are only consumed by the new-design atoms, never by widgets or the old design. --line-height-body changed from a fixed 21px to unitless 1.3125 so it scales proportionally with the now-fluid font-size. | Bug Fix | src/components/MarqueeTag.tsx, src/app/page.module.css, src/app/page.tsx, src/app/globals.css, internal/DESIGN.md |
| 20 | Marquee bottom-align, dev resolution overlay, new bg image | (1) .marqueeSlot changed align-self from center to flex-end, matching the other header pills' baseline. (2) Added ResolutionIndicator — a small dev-only fixed overlay (bottom-right) showing live window.innerWidth x innerHeight, for reference while testing responsiveness; flagged with a TODO to remove before final ship. (3) Swapped the header background texture from the placeholder homepage-bg-texture.png to the user-supplied public/assets/Website Bg.png (1920x1080 dot-grid), sized to match and opacity removed since the new asset is already designed at full strength. | Update | src/app/page.module.css, src/app/page.tsx, src/components/ResolutionIndicator.tsx, src/components/ResolutionIndicator.module.css |
| 21 | Font-size dip added for 1400-1700px viewport range | --font-size-md and --font-size-body now dip down to 12px (interpolated, not a hard jump) across the 1700px→1400px viewport range, pinned at 12px below 1400px, resuming the normal 768-1920px fluid clamp() curve above 1700px. Implemented via two @media (max-width) overrides of the :root custom properties, since a single clamp() can't express a non-monotonic curve. Verified the interpolation math directly (19.24px at 1700, 15.62px at the 1550 midpoint, 12px at 1400) — could not visually confirm via browser resize since the sandboxed browser's window.innerWidth doesn't respond to the resize_window tool in this environment. | Update | src/app/globals.css, internal/DESIGN.md |
| 22 | Join screen built (/join) from Figma (node 157:1634) | Assembled the join/apply screen, reusing the homepage's exact header and right column plus the same column ratio (400:899:435). Extracted shared pieces so both screens stay in sync: SiteHeader (header row + real member count), SiteInfoColumn (author's note/site stats/update widget), lib/version.ts (getLatestVersion), shared screenLayout.module.css (page shell + column flex ratios) replacing the old page.module.css duplication. Left column: 3 TaggedSections (join the webring/how it works/return home). Center column: built WidgetPreviewPanel — reuses WidgetTicker's registry + prev/next cycling logic but restyled as a slim TaggedSection-style box driven by NavTag pills instead of WidgetTicker's built-in hover-carets (per user's explicit choice, since WidgetTicker's own 220px panel didn't match Figma's compact pill look). Reused FormSection fully functional (real /api/apply POST, real validation) — note: FormSection keeps its legacy fixed 919px width and its own bordered submit/discard buttons (not restyled to NavTag pills), a known deviation accepted in exchange for zero risk to its working submission logic. JoinFormClient wires nickname + selected widgetId as shared state between the preview and the form. Homepage's "click to join" links now real next/link to /join (previously TODO placeholders). | Feature | src/app/join/page.tsx, src/components/sections/SiteHeader.tsx, src/components/sections/SiteHeader.module.css, src/components/sections/SiteInfoColumn.tsx, src/components/sections/WidgetPreviewPanel.tsx, src/components/sections/WidgetPreviewPanel.module.css, src/components/sections/JoinFormClient.tsx, src/components/sections/JoinFormClient.module.css, src/lib/version.ts, src/styles/screenLayout.module.css, src/app/page.tsx, src/app/page.module.css |
| 23 | Form full-width + NavTag submit/cancel (Figma node 199:758) + widget preview migrated to v2, legacy widget system deleted | FormSection.container changed from a hardcoded 919px to width:100%; submit/discard buttons replaced with real NavTag components ("SUBMIT >>"/"CANCEL >>", matching Figma exactly) instead of the old bordered "Submit The Application"/"Discard & Go Back" buttons — same onClick/validation/POST logic untouched. WidgetPreviewPanel switched from legacy WidgetRenderer/11-widget registry to WidgetV2Renderer/widgetV2Registry (9 widgets), and its nav row changed from centered to left-aligned (align-items: flex-start), matching the box's left edge per Figma. Deleting the legacy widgets turned out to be a live-production change, not just a dev-preview swap: discovered /app/widget/[slug] (the real script-embed route) and /app/embed/[slug] (legacy iframe route) both rendered legacy widgets, real member "imti" had widget:009 (legacy) actively embedded on imtiyazahmed.com, and the mobile join/success flow (MobileFormPage/MobileSuccessPage/MobileRoot, reachable from the homepage on narrow screens) also depended on legacy WidgetRenderer. Migrated all of it to v2 before deleting: both production routes, both mobile files, imti.md's widget id (009 -> 001), plus a new src/lib/widgetV2Sizes.ts (approximate per-widget width/height for embed scaling, since no v2 equivalent of the legacy widgetSizes.ts existed). Deleted: src/widgets/Widget001-011.tsx + .module.css, WidgetRenderer.tsx, root registry.ts, src/lib/widgetSizes.ts, WidgetTicker.tsx/.module.css, CenterColumn.tsx (fully orphaned once the homepage stopped using it, and its import of WidgetTicker/WidgetRenderer would have been a broken build otherwise), and the /app/widgets dev-preview route. Verified with `tsc --noEmit` (zero errors) and confirmed no remaining references before deleting. | Bug Fix / Feature | src/components/sections/FormSection.tsx, src/components/sections/FormSection.module.css, src/components/sections/WidgetPreviewPanel.tsx, src/components/sections/WidgetPreviewPanel.module.css, src/lib/widgetV2Sizes.ts, src/app/widget/[slug]/page.tsx, src/app/embed/[slug]/page.tsx, src/components/mobile/MobileFormPage.tsx, src/components/mobile/MobileSuccessPage.tsx, src/components/mobile/MobileRoot.tsx, members/imti.md, (deleted) src/widgets/Widget001-011.tsx+css, src/widgets/WidgetRenderer.tsx, registry.ts, src/lib/widgetSizes.ts, src/components/sections/WidgetTicker.tsx+css, src/components/CenterColumn.tsx, src/app/widgets/page.tsx |
| 24 | Form field colors corrected to match Figma exactly (node 200:759) | Found the color roles were inverted: FormSection's outer .part group had a border but no fill (showing page bg through), while individual .field inputs had a surface-color fill — Figma does the opposite. Fixed: .part is now var(--color-surface) with no border and var(--space-4) padding (was var(--space-6)); .field is now transparent (border only), letting the surface color show through. Added new token --color-text-placeholder (#676767) — placeholder text is a distinctly darker grey than --color-text-secondary, not an existing token. Also corrected .fieldTextarea height from 48px to the Figma-exact 65px, and added a 4px gap (var(--space-1)) between the two field groups matching Figma's spacing. | Bug Fix | src/components/sections/FormSection.module.css, src/app/globals.css, internal/DESIGN.md |
| 25 | Join screen header color swap + fixed-height widget preview | "select a widget"/"fill the details" header tags changed from blue to pink; "update widget" (right column) changed from pink to blue. WidgetPreviewPanel's .previewBox given an explicit height:200px + flex-shrink:0 so it stays fixed regardless of viewport (was auto-height, growing/shrinking with content) — widget stays centered within via existing align-items/justify-content:center. | Update | src/components/sections/JoinFormClient.tsx, src/components/sections/SiteInfoColumn.tsx, src/components/sections/WidgetPreviewPanel.module.css |
| 26 | Live nickname + custom color preview wired; nickname growth centered across all v2 widgets | Confirmed nickname live-preview was already wired correctly. Added live preview for custom background/text color: new onBgColorChange/onTextColorChange callbacks on FormSection, threaded through JoinFormClient -> WidgetPreviewPanel -> WidgetV2Renderer (same pattern as nickname) -> each widget's --widget-bg/--widget-text CSS vars, which the MisfitsMark icon already inherits via currentColor. Audited all 9 v2 widgets for how their nickname-containing identity box grows as text lengthens: widget-001 was right-anchored (align-items:flex-end + fixed margin-right:48px, growing leftward) and widget-003 was explicitly left-anchored (align-self:flex-start) — both changed to center-anchored growth. Widgets 002/005/009 already centered correctly (no change); widgets 004/006/007/008 don't render the nickname at all (static text), which is expected/fine per user. | Bug Fix / Feature | src/components/sections/FormSection.tsx, src/components/sections/WidgetPreviewPanel.tsx, src/components/sections/JoinFormClient.tsx, src/widgets/v2/widget-001.module.css, src/widgets/v2/widget-003.module.css |
| 27 | Color input length cap, form font matched to section body copy, widget preview scale-bounce transition | Custom background/text color inputs capped at maxLength=7 ("#" + 6 hex digits), matching the existing nickname maxLength pattern. FormSection's .field font changed from Inter/--font-size-sm(14px, fixed) to Chivo/--font-size-body(fluid, same as TaggedSection's paragraph text) for consistency across the screen — since --font-size-body can reach 18px, .field/.fieldTextarea switched from fixed height to min-height to avoid clipping at the larger end of the fluid range. WidgetPreviewPanel: clicking prev/next no longer swaps the widget instantly — it now scales down to 0.6/fades out (150ms), swaps the underlying widget at the trough, then scales back up with a slight overshoot bounce (350ms, cubic-bezier overshoot curve) via two new keyframe animations (scaleOut/scaleInBounce), with setTimeout-driven phase state cleaned up on unmount. | Update | src/components/sections/FormSection.tsx, src/components/sections/FormSection.module.css, src/components/sections/WidgetPreviewPanel.tsx, src/components/sections/WidgetPreviewPanel.module.css |
| 28 | Update Widget copy rewritten | Replaced the "Click This Link" placeholder (which had no real destination) with plain descriptive copy about an upcoming feature: letting existing members update their widget design without resubmitting the form. Header's "Click to Join" link was already wired to /join from an earlier session (via SiteHeader), confirmed no change needed. | Update | src/components/sections/SiteInfoColumn.tsx |
| 29 | Widget preview scale-bounce animation toned down | Scale-out reduced from 0.6 to 0.85, bounce overshoot reduced from 1.08 to 1.03 (dip 0.96 -> 0.99) — presented as 3 metric options (subtle/moderate/light-touch) per CLAUDE.md's metric-change rule; subtle was chosen. Durations (150ms out / 350ms in) unchanged. | Update | src/components/sections/WidgetPreviewPanel.module.css |
| 30 | Restricted host-page widget override to width + font only; fixed preview overflow at 1400-1700px | widget-loader.js now reads optional data-width/data-font attributes off the embedding site's <script> tag — data-width scales the whole widget proportionally to fit (measuring natural rendered width, then applying a single transform:scale, no distortion), data-font force-overrides font-family via an injected !important shadow-DOM style. Nothing else (colors, layout, content) is overridable from the host page — those stay fixed from the member's join-form submission. Also fixed WidgetPreviewPanel overflowing its box in the 1400-1700px viewport dip (widget-001 is 752px wide, wider than the box at that squeezed width): added a persistent scale-to-fit computed via ResizeObserver (measuring the unscaled widget via scrollWidth, comparing to the box's available width), applied on a separate nested wrapper from the bounce animation's own transform so the two don't fight over the same CSS property; added overflow:hidden to previewBox as a safety net. Added a "How It Works?" bullet on the join screen describing the new width/font override capability. | Feature / Bug Fix | public/widget-loader.js, src/components/sections/WidgetPreviewPanel.tsx, src/components/sections/WidgetPreviewPanel.module.css, src/app/join/page.tsx |
| 31 | Fixed two regressions: widget-001 badge alignment reverted, scale-to-fit now reacts to nickname/color changes | (1) The previous session's "center the nickname growth" fix moved widget-001's badge to the dead center of its 752px-wide bar — a big, wrong positional shift from its actual right-anchored Figma design (badge near the bar's right edge, offset 48px). Reverted to align-items:flex-end + margin-right:48px. (2) The scale-to-fit ResizeObserver only watched the outer preview box, not the widget content itself — so a widget growing wider from a longer nickname (on any widget, not just 001) wouldn't retrigger the fit calculation and could silently overflow again after the initial correct render. Now observes both the container and the measured widget element. | Bug Fix | src/widgets/v2/widget-001.module.css, src/components/sections/WidgetPreviewPanel.tsx |
| 32 | Widget embed code stored in member frontmatter | The apply route already generated the `<script>` embed snippet client-side for the success screen but never persisted it. Now written into each member's markdown frontmatter as `embedCode` at submission time (and backfilled into members/imti.md), so it's available anywhere member records are read without regenerating it. | Feature | src/app/api/apply/route.ts, members/imti.md |
| 33 | Removed dead legacy UI components | 9 section components (AboutSection, AnimationSection, AuthorSection, JoinGuildSection, JoinSection, RequirementSection, SuccessSection, PreviewSection, GallerySection) had zero remaining imports anywhere — orphaned by the redesign but never deleted. Confirmed via grep before removing. Kept GallerySection.module.css since GalleryTrack.tsx (still in use) pulls its .track/.imageItem classes from it. | Refactor | (deleted) 9x .tsx + .module.css pairs under src/components/sections/ |
| 34 | Custom pixel cursor | New 12px pixel-square cursor (CustomCursor.tsx) replacing the native pointer on desktop (pointer:fine, skips /widget and /embed since those render inside third-party iframes). Defaults to the blue accent, cycles to the next accent color on click, swaps to a contrasting color while hovering any element whose background matches its current color, leaves a fading pixel trail, and bursts into small pixel particles (12 per click, randomized size/spread, ease-out cubic-bezier) when clicking a link or button. Tuned across several passes for burst size and animation smoothness/speed. | Feature | src/components/CustomCursor.tsx, src/components/CustomCursor.module.css, src/app/layout.tsx, src/app/globals.css |
| 35 | Favicon swapped | Replaced the Webring Logo placeholder favicon with the dedicated Favicon.png asset. | Update | src/app/layout.tsx, public/assets/Favicon.png |
| 36 | Page-level screen transitions (fade + scale + bounce, GSAP-based) | First attempt used the browser View Transitions API, which proved unreliable across Next.js App Router's async navigation (snapshot captured before the new route actually rendered — "bounces, content doesn't change") and was dropped entirely in favor of a fully JS-controlled approach. PageTransition.tsx now wraps page content, intercepts internal link clicks, plays an instant uniform exit (all elements fade/scale out together, 0.1s) before calling router.push, waits for the route to actually change (via usePathname), then plays a staggered entrance: the nav bar (data-transition-group="header") first, then each card (data-transition-group="card", tagged on TaggedSection/JoinFormClient's widget+form groups) individually with a 45ms stagger and back-out bounce easing — sorted top-to-bottom by actual on-screen position rather than DOM/column order. | Feature | src/components/PageTransition.tsx, src/app/layout.tsx, src/app/globals.css, src/components/TaggedSection.tsx, src/components/sections/SiteHeader.tsx, src/components/sections/JoinFormClient.tsx |
| 37 | GalleryCard component + Gallery page (Figma nodes 208:795, 191:367) | GalleryCard: member site screenshot (5:3 crop, object-fit:cover) with a dark name-tag pill stacked below (4px gap), whole card links out to the member's site via existing MemberLink (external, new tab). Hover randomizes the tag's fill across the 5 accent colors, with text flipping to black on yellow/green and staying white on purple/blue/pink. New /gallery route: reuses the shared header + TaggedSection info card ("gallery of sites", link back home), grid of GalleryCards for every member with a saved R2 screenshot. Wired the previously dead "gallery >" header nav link to /gallery. | Feature | src/components/GalleryCard.tsx, src/components/GalleryCard.module.css, src/app/gallery/page.tsx, src/app/gallery/gallery.module.css, src/components/sections/SiteHeader.tsx |
| 38 | Form: optional tags, auto-prefixed https://, success panel replaces form in place (Figma node 211:796) | Tags field is no longer required (validation + placeholder updated). Website Link input now auto-prefixes `https://` as the user starts typing, mirroring the existing `#` auto-prefix on the color fields, with backspace-safe handling so a partial "https://" being typed or deleted isn't fought by the prefix logic. On successful submission, JoinFormClient animates the center section out and the new WidgetSuccessPanel in at the same spot, using the same exit/enter timing as the page-level transition. WidgetSuccessPanel shows the final widget preview (whatever widget/nickname/colors the user picked, read-only) plus the real `<script>` embed code (not the iframe shown in the Figma mock) in a bordered, click-to-copy code box, with a second "copy the url >>" button below. | Feature | src/components/sections/FormSection.tsx, src/components/sections/JoinFormClient.tsx, src/components/sections/WidgetSuccessPanel.tsx, src/components/sections/WidgetSuccessPanel.module.css |
| 39 | Site stats wired to real data | Days Online now computed from a hardcoded launch-date constant (2026-07-25, clamped at 0 until that date) instead of a static placeholder — no DB schema change needed, per the project rule against touching schema without permission. Last Update now fetches the latest commit's timestamp from the GitHub REST API (same repo/token convention as the apply route), covering merged PRs too since a merge is itself a commit on main; cached for 1 hour. Version was already wired to RELEASES.md and needed no code change. v1.0.0 release entry intentionally not added yet — launch is planned for a later date. | Feature | src/lib/version.ts, src/app/page.tsx, src/app/join/page.tsx, src/components/sections/SiteInfoColumn.tsx |
| 40 | Pending-member cleanup fixed for the common (widget-selected) case; `members-pending/` moved to `members/pending/` | Found that the pending-preview file written straight to `main` at signup was only ever cleaned up through `WidgetPending`'s polling loop, which is exclusively rendered when a member hasn't picked a widget yet — members who do pick one (the normal case) render straight into `WidgetV2Renderer` with a `pending` flag and nothing ever polled `/api/member/[slug]/status`, so their pending file lingered in the repo forever after approval. Extracted the polling logic into a shared hook (`usePendingApprovalPoll`) and added an invisible `PendingApprovalSync` component that mounts alongside the widget whenever `pending` is true, so both cases now trigger the existing GitHub delete once the real `members/{slug}.md` file exists. Also moved the pending-preview folder from a sibling `members-pending/` to `members/pending/`, nested under the members folder as requested — confirmed the non-recursive `readdirSync` calls in `src/lib/sites.ts`, `scripts/check-members.mjs`, and `scripts/flush-last-seen.mjs` won't pick up the nested subfolder as a real member. | Bug Fix | src/app/api/apply/route.ts, src/app/api/member/[slug]/status/route.ts, src/app/embed/[slug]/page.tsx, src/widgets/usePendingApprovalPoll.ts, src/widgets/PendingApprovalSync.tsx, src/widgets/WidgetPending.tsx |
| 41 | Gallery card border + sharper screenshots; shuffle icon swapped | GalleryCard's `.imageWrap` now has a 1px white border per Figma. Investigated the "blurry in the gallery, sharp when opened directly" report: screenshots are captured via ScreenshotOne at 1280x800 with `device_scale_factor` left at its 1x default, and gallery cards can render wider than that on desktop when there are only a few members — the browser was upscaling past the source resolution. Bumped capture to `device_scale_factor=2` and `image_quality` 85→90; only affects screenshots taken for new applications going forward, existing members' R2 screenshots aren't retroactively re-captured. Separately, replaced the `ShuffleIcon` path data used across widgets 004/006/007/008/009 with a new icon supplied by the user. | Bug Fix / Update | src/components/GalleryCard.module.css, src/app/api/apply/route.ts, src/widgets/v2/icons.tsx |

---

## Session 6 — 2026-07-24

| # | Title | Description | Type | Files Changed |
|---|-------|-------------|------|---------------|
| 42 | Mobile screens rebuilt from Figma (nodes 228:1065, 227:304, 245:425, 245:562) — header/section tags now reuse real desktop components; new mobile Gallery screen | Root cause of the "mobile tags are thinner than desktop" report: mobile never used the shared HeaderTag/StackedTag/LogoBox/NavTag components at all — it had its own hand-rolled bordered strip + plain-text tab row. Rewrote MobileHeader to compose LogoBox + HeaderTag ("gallery >") + StackedTag (active/members, click to join), matching desktop's SiteHeader exactly; dropped the old About/Criteria/Join/Members text-tab row since it isn't in the Figma mobile hierarchy. Added a `fullWidth` prop to HeaderTag/TaggedSection (default false, so desktop is untouched) so the same components can render as full-width centered bars on mobile per Figma, instead of desktop's left-aligned hug-content pill. Recolored every mobile section header to match its real desktop counterpart exactly (verified against page.tsx, join/page.tsx, JoinFormClient.tsx, SiteInfoColumn.tsx) rather than Figma's mobile-mock colors where the two disagreed (e.g. select-a-widget/fill-the-details are pink on desktop, was blue in the Figma mobile mock — went with desktop per explicit user instruction). Prev/Next and Submit/Cancel are now the real NavTag pill component (previously custom-styled buttons). Reused TaggedSection, SiteInfoColumn, and GalleryCard directly instead of re-implementing their content — guarantees mobile stays in sync with desktop copy/data automatically. Extracted MobileInfoSections (How It Works / Return Home / SiteInfoColumn) shared by both the join form and success screens, replacing the old bottom dump of Members/Gallery-marquee/Criteria (not part of either Figma screen). Built new MobileGalleryPage — previously gallery had no mobile view at all — wired into gallery/page.tsx's existing desktop/mobile split. Home's standalone gallery marquee and interactive dot-grid demo were dropped from the mobile homepage (not present in the Figma mobile hierarchy; gallery now lives on its own screen, reachable from the header). Threaded live site data (views/memberCount/latestVersion/daysOnline/lastUpdate) down through MobileRoot as a single `siteInfo` prop instead of a precomputed `formBottomContent` ReactNode, since the new shared info sections need an `onGoBack` client callback that a server-rendered node couldn't carry. Deleted the now-unused MobileFormBottomContent. Verified with `tsc --noEmit` and eslint (zero errors); visually spot-checked header/section colors, the join flow, and the new gallery screen in-browser. Could not get a genuine narrow viewport in this environment (resize_window didn't affect actual rendering) — used a DOM/CSS override hack to approximate 393px for review, which also revealed the widget ticker measuring 0×0 under that artificial setup; confirmed the identical WidgetV2Renderer renders correctly on the real desktop /join page, so this reads as a test-harness artifact rather than a regression, but flagged for the user to double check on a real device. | Feature | src/components/mobile/MobileHeader.tsx, src/components/mobile/MobileHeader.module.css, src/components/mobile/MobilePage.tsx, src/components/mobile/MobilePage.module.css, src/components/mobile/MobileFormPage.tsx, src/components/mobile/MobileFormPage.module.css, src/components/mobile/MobileSuccessPage.tsx, src/components/mobile/MobileSuccessPage.module.css, src/components/mobile/MobileGalleryPage.tsx, src/components/mobile/MobileInfoSections.tsx, src/components/mobile/MobileReturnHomeButton.tsx, src/components/mobile/MobileJoinButton.tsx, src/components/mobile/MobileJoinButton.module.css, src/components/mobile/MobileRoot.tsx, src/components/HeaderTag.tsx, src/components/HeaderTag.module.css, src/components/TaggedSection.tsx, src/components/TaggedSection.module.css, src/components/sections/SiteInfoColumn.tsx, src/app/page.tsx, src/app/gallery/page.tsx, (deleted) src/components/mobile/MobileFormBottomContent.tsx |

---

_Types: Setup · Feature · Bug Fix · Update · Refactor · Config · Copy_
